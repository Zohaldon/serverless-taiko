// Pull the Shopify's access token for this particular integration environment.
async function getTitleName() {
  const token = 'Log in - Shopify Account'
  return token;
}

module.exports = {
  getTitleName
}